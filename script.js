document.addEventListener("DOMContentLoaded",function(){const navbar=document.querySelector(".navbar");const navLinks=document.querySelectorAll(".nav-menu .nav-link");const sections=document.querySelectorAll("section[id]");function handleScroll(){if(window.scrollY>50){navbar.classList.add("scrolled")}else{navbar.classList.remove("scrolled")}let currentSectionId="";sections.forEach(section=>{const sectionTop=section.offsetTop-navbar.offsetHeight-50;const sectionHeight=section.offsetHeight;if(window.scrollY>=sectionTop&&window.scrollY<sectionTop+sectionHeight){currentSectionId=section.getAttribute("id")}});navLinks.forEach(link=>{link.classList.remove("active");if(link.getAttribute("href")===`#${currentSectionId}`){link.classList.add("active")}});if(!currentSectionId&&window.scrollY<sections[0].offsetTop){const homeLink=document.querySelector('.nav-menu a[href="#home"]');if(homeLink)homeLink.classList.add("active")}}window.addEventListener("scroll",handleScroll);const navToggleButton=document.getElementById("nav-toggle-button");const navMenuItems=document.getElementById("nav-menu-items");if(navToggleButton&&navMenuItems){navToggleButton.addEventListener("click",()=>{navMenuItems.classList.toggle("open");const isExpanded=navMenuItems.classList.contains("open");navToggleButton.setAttribute("aria-expanded",isExpanded);navToggleButton.classList.toggle("open")});navLinks.forEach(link=>{link.addEventListener("click",()=>{if(navMenuItems.classList.contains("open")){navMenuItems.classList.remove("open");navToggleButton.setAttribute("aria-expanded","false");navToggleButton.classList.remove("open")}})})}const faqItems=document.querySelectorAll(".faq-item");faqItems.forEach(item=>{const questionButton=item.querySelector(".faq-question");const answerDiv=item.querySelector(".faq-answer");if(questionButton&&answerDiv){questionButton.addEventListener("click",()=>{const isOpen=item.classList.contains("open");item.classList.toggle("open");questionButton.setAttribute("aria-expanded",!isOpen);if(!isOpen){answerDiv.style.maxHeight=answerDiv.scrollHeight+"px"}else{answerDiv.style.maxHeight=null}})}});const backToTopButton=document.getElementById("back-to-top-button");if(backToTopButton){window.addEventListener("scroll",()=>{if(window.scrollY>300){backToTopButton.classList.add("show")}else{backToTopButton.classList.remove("show")}});backToTopButton.addEventListener("click",e=>{e.preventDefault();window.scrollTo({top:0,behavior:"smooth"})})}const yearSpan=document.getElementById("year");if(yearSpan){yearSpan.textContent=(new Date).getFullYear()}const galleryContainer=document.getElementById("gallery-container");if(galleryContainer){galleryContainer.addEventListener("click",function(event){const galleryLink=event.target.closest(".gallery-item");if(galleryLink){event.preventDefault();const imageSrc=galleryLink.getAttribute("href");if(typeof basicLightbox!=="undefined"){const instance=basicLightbox.create(`
                        <img src="${imageSrc}" style="max-width: 90vw; max-height: 90vh;">
                    `);instance.show()}else{console.error("basicLightbox library is not loaded.")}}})}});AOS.init({duration:800,once:true,offset:50,easing:"ease-in-out"});
